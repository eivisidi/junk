cdparanoia:
cdparanoia -vsQ (for info)
cdparanoia -B (rip all tracks to current directory)

ufw:
sudo ufw enable

sudo:
su chmod u+w /etc/sudoers
vim /etc/sudoers
vasago ALL=(ALL) ALL (at the end)
chmod u-w /etc/sudoers

sources list:  
su vim /etc/apt/sources.list
change squeeze to testing
add main contrib non-free 
keep security in stable

dwm:
mkdir ~/Builds
cd Builds
apt-get build-dep dwm
apt-get source dwm 
or
wget http://dl.suckless.org/dwm/dwm-version.tar.gz
tar -xvf dwm-version.tar.gz
cd dwm-version
cp config.def.h config.h (configure)
sudo make clean install

configure brightness:
su
echo 100 > /proc/acpi/video/GFX0/DD02/brightness

tips:
cat file1 >> file2 (merge files)
shif+insert (mouse middle click)
dmesg | tail
fdisk -l  
sudo ./truecrypt (execute program)
rcconf: atd, rpcbind, portmap, rsync, sudo, rsyslog, exim4 (uncheck)
du -a file (file size)
cp -r directoryfile /etc/whatever (copy all directory and subdirectories to..)
gphoto2 --auto-detect
gphoto2 -P (rip all camera photos to current folder)

format usb:
insert pendrive
fdisk -l or dmesg | tail
ex. /dev/sdb1
sudo umount /dev/sdb1
sudo mkfs.vfat -n 'toshiba' -l /dev/sdb1
sudo mkfs.ntfs -n 'toshiba' -l /dev/sdb1

.bashrc:
PS1='\[\033[1;32m\]-->\033[0m\] '
