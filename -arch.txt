1. 
gparted

2. 
wifi-menu
ping -c 3 www.google.com

3.
lsblk /dev/sdb
mount /dev/sdb1 /mnt
mkdir /mnt/home
mount /dev/sdb2 /mnt/home
swapon /dev/sdb3
	
3.
pacstrap -i /mnt base base-devel grub-bios

if corrupted message appears:
pacman-key --init && pacman-key --populate archlinux 

pacman -Syy
pacman -Syu

4.
genfstab -U -p /mnt >> /mnt/etc/fstab

5.
arch-chroot /mnt

6.
vi /etc/locale.gen
uncomment: 
en_US.UTF-8 UTF-8 
en_US ISO-8859-1  
echo LANG=en_US.UTF-8 > /etc/locale.conf
export LANG=en_US.UTF-8

7.
setfont Lat2-Terminus16
vi /etc/vconsole.conf
KEYMAP=us
FONT=Lat2-Terminus16

8.
timedatectl status
timedatectl list-timezones
timedatectl set-timezone America/Puerto_Rico

9.
grub-install --recheck /dev/sdb
grub-mkconfig -o /boot/grub/grub.cfg
cp /usr/share/locale/en\@quot/LC_MESSAGES/grub.mo /boot/grub/locale/en.mo

10.
echo jurasik > /etc/hostname
passwd
useradd -m -g users -G audio,disk,games,locate,lp,network,optical,power,scanner,storage,sys,users,uucp,video,wheel -s /bin/bash vicom
passwd vicom

11.
less /usr/share/X11/xkb/rules/base.lst (choose layout)
sudo vi /etc/X11/xorg.conf.d/10-keyboard.conf
Section "InputClass"
    Identifier "Keyboard Defaults"
    MatchIsKeyboard "yes"
    Option "XkbLayout" "us"
    Option "XkbVariant" "intl"
EndSection
ZZ

sudo vi /etc/X11/xorg.conf.d/20-intel.conf 
Section "Device"
   Identifier  "Intel Graphics"
   Driver      "intel"
   Option      "AccelMethod"  "uxa"
EndSection
ZZ

12.
pacman -S alsa-utils audacious cinnamon clamav dialog evince faenza-icon-theme gedit gimp gparted intel-ucode iw libreoffice-en-US libreoffice-gnome libreoffice-writer lightdm lightdm-gtk3-greeter lsb-release mesa mlocate nemo-fileroller p7zip pidgin shotwell skype terminator transmission-gtk truecrypt ttf-dejavu ttf-ubuntu-font-family ufw unrar unzip viewnior vlc wireless_tools wpa_actiond wpa_supplicant xf86-input-synaptics xf86-video-intel xf86-video-vesa xorg-server xorg-server-utils xorg-xclock xorg-xinit xorg-twm

13.
su
chmod ugo+w /etc/sudoers
vi /etc/sudoers
## User privilege specification
root ALL=(ALL) ALL
username ALL=(ALL) ALL
ZZ
chmod ugo-w /etc/sudoers

14.
sudo vi /etc/pacman.conf

[archlinuxfr]
SigLevel = Never
Server = http://repo.archlinux.fr/$arch
ZZ

sudo pacman -Syu
sudo pacman -S yaourt
sudo yaourt -S google-musicmanager xcursor-chameleon-anthracite

15.
sudo vi /etc/lightdm/lightdm.conf
uncomment:
greeter-user=lightdm
greeter-hide-users=false
ZZ

sudo vi /etc/lightdm/lightdm-gtk-greeter.conf
uncomment:
background=/usr/share/pixmaps/triangle.png
theme-name=Void
icon-theme-name=Faenza
font-name=Ubuntu
xft-antialias=true
xft-dpi=96
xft-hintstyle=full
xft-rgba=rgb
show-language-selector=true
show-clock=true
clock-format=%H:%M
ZZ

sudo systemctl enable lightdm

16.
sudo ufw enable
sudo systemctl enable ufw

sudo updatedb

sudo vi /etc/clamav/freshclam.conf
comment Example: 
# Example
ZZ
sudo vi /etc/clamav/clamd.conf 
comment Example: 
# Example
ZZ
sudo freshclam (update)
sudo clamscan myfile (scan for virus)

17.
terminal
wifi-menu
iwconfig
systemctl enable netctl-auto@wlp1s2.service
reboot

18.
cinnamon system settings
applets
get more online
netctl systray menu
