aptitude install git xclip
git config --global user.name "John Doe"
git config --global user.email johndoe@gmail.com
ssh key:
cd ~/.ssh
ssh-keygen -t rsa -C "johndoe@gmail.com"
xclip -sel clip < ~/.ssh/id_rsa.pub
or vim ~/.ssh/id_rsa.pub 
copy key (begins with ssh-rsa.. ends with username.com)
github.com --> account settings --> ssh keys -->
add ssh key --> paste key --> add key
ssh -T git@github.com
yes

First time:
mkdir junk
cd junk
git init
touch filename
git add filename
git commit -m 'first commit'
git remote add origin git@github.com:username/junk.git
git push origin master

Rest of the times:
mkdir Build
cd Build 
git init
git clone git@github.com:username/junk.git
cd junk
edit file
git add editedfile (one)
git commit -m 'first commit' (two)
git push origin master (three)

Remove file:
git rm filename
git commit -m 'first commit' (two)
git push origin master (three)
