1. Iso
https://cdimage.debian.org/cdimage/unofficial/non-free/cd-including-firmware/
netinstall non-free debian (standard system utilities)

2. Network
ip link (examples: wlan0, wlp12s0, wlp3s0)
su
vi /etc/network/interfaces
auto wlan0
iface wlan0 inet dhcp
    wpa-ssid essid
    wpa-psk password
ZZ
chmod 600 /etc/network (if needed)
reboot
ping -c 3 google.com

3. Sudo
su
apt update; apt full-upgrade
apt install sudo
reboot
su
/sbin/adduser username sudo
reboot

4. Boot last forever (if needed)
sudo vi /etc/default/grub
from:
GRUB_CMDLINE_LINUX_DEFAULT="quiet"
to:
GRUB_CMDLINE_LINUX_DEFAULT="quiet video=SVIDEO-1:d"
ZZ
sudo update-grub

5. Repositories
su
vi /etc/apt/sources.list
# Base repository
deb http://deb.debian.org/debian/ buster main contrib non-free
deb-src http://deb.debian.org/debian/ buster main contrib non-free

# Security updates
deb http://security.debian.org/debian-security buster/updates main contrib non-free
deb-src http://security.debian.org/debian-security buster/updates main contrib non-free

# Stable updates
deb http://deb.debian.org/debian buster-updates main contrib non-free
deb-src http://deb.debian.org/debian buster-updates main contrib non-free

# Stable backports
# apt -t buster-backports install PACKAGE
deb http://deb.debian.org/debian buster-backports main contrib non-free
deb-src http://deb.debian.org/debian buster-backports main contrib non-free
ZZ

6. Keyboard/Font
sudo dpkg-reconfigure keyboard-configuration (US Int with dead keys)
sudo dpkg-reconfigure console-setup (Terminus 10x20)

7. Ssd
sudo systemctl enable fstrim.timer

8. Mlocate
sudo apt install mlocate; sudo /etc/cron.daily/mlocate

9. Firewall
sudo apt install ufw
sudo ufw enable

10. Extend laptop battery life
sudo apt install tlp
reboot
sudo systemctl status tlp

11. Restricted extras packages plus extras
sudo apt install aptitude gstreamer1.0-libav gstreamer1.0-plugins-ugly 
gstreamer1.0-vaapi  intel-microcode libavcodec-extra p7zip-full p7zip-rar 
rar unrar wget

12. Gnome
sudo aptitude install arc-theme chameleon-cursor-theme cups dconf-editor file-roller fonts-roboto gnome-multi-writer gdebi gnome-core gparted hplip keepassx nautilus-gtkhash nautilus-image-converter papirus-icon-theme seahorse-nautilus shotwell simple-scan system-config-printer timeshift transmission-gtk vlc xfonts-terminus

13. Transfer from network interfaces to network-manager
su
vi /etc/network/interfaces
comment out everything:
# auto wlan0
# iface wlan0 inet dhcp
#    wpa-ssid essid
#    wpa-psk password
ZZ

14. Bluetooth
sudo systemctl disable bluetooth
systemctl is-enabled bluetooth
sudo systemctl enable bluetooth

15. Gnome extensions (extensions.gnome.org):
• arc menu
• dash to panel                  
• clock override (%m.%d.%C        %H:%M)
• open weather
• removable drive menu
• user themes

16. Manual install
pia (https://www.privateinternetaccess.com/pages/download)
sh pia-linux-2.1-04977
plex (https://www.plex.tv/pt/media-server-downloads/)

17. Shortcuts
gnome-session-quit --power-off (Ctrl + Esc)
gnome-terminal (Ctrl + Enter)

0. Reference
Vi
i	Switch to Insert mode.
Esc	Switch to Command mode.
:w	Save and continue editing.
:ZZ     Save and quit/exit vi.
:q!	Quit vi and do not save changes.
yy	Yank (copy a line of text).
p	Paste a line of yanked text below the current line.
o	Open a new line under the current line.
O	Open a new line above the current line.
A	Append to the end of the line.
a	Append after the cursor's current position.
I	Insert text at the beginning of the current line.
b	Go to the beginning of the word.
e	Go to the end of the word.
x	Delete a single character.
dd	Delete an entire line.
Xdd Delete X number of lines.
Xyy	Yank X number of lines.
G	Go to the last line in a file.
XG	Go to line X in a file.
gg	Go to the first line in a file.
:numDisplay the current line's line number.
h	Move left one character.
j	Move down one line.
k	Move up one line.
l	Move right one character.
