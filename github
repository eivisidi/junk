cd ~/.ssh
"no such file or directory"

when there is a directory:
ls
mkdir key_backup
cp id_rsa* key_backup
rm id_rsa

if not, then:
ssh-keygen -t rsa -C "email"
enter
enter passphrase
confirm passphrase

on github:
account settings
ssh public keys
add another public key
vim ~/.ssh/id_rsa.pub
copy key to "add another key" on github 
(begins with ssh-rsa.. ends with username.com)
add key
ssh -T git@github.com
yes
password
enter
git config --global user.name "firstname lastname"
git config --global user.email "email"
account settings
account admin
copy api token
git config --global github.user "username"
git config --global github.token "api token"

create repo:
on github click "new repository"
proyect name
create repository
mkdir ~/junk
cd junk
git init
touch config.h (or put whatever it is you want to upload)
git add config.h
git commit -m 'first commit'
git remote add origin git@github.com:username/junk.git
git push origin master




